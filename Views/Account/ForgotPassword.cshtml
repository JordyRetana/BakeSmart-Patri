@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Recuperar contraseña";
}

@section Styles {
    <link rel="stylesheet" href="/css/pages/account-password.css?v=20260807-final" />
}

<div class="login-page-shell password-page-shell">
    <div class="card login-card password-card">
        <div class="login-hero">
            <div class="login-logo-wrap">
                <i class="fas fa-key"></i>
            </div>

            <h2 class="login-title">Recuperar contraseña</h2>
            <p class="text-muted login-subtitle">
                Ingresa tu correo y recibirás un enlace seguro para crear una nueva contraseña.
            </p>
        </div>

        @if (!string.IsNullOrEmpty(TempData["ToastError"]?.ToString()))
        {
            <div class="alert alert-error login-alert">
                <i class="fas fa-exclamation-circle"></i>
                @TempData["ToastError"]
            </div>
        }

        @if (!string.IsNullOrEmpty(ViewData["ResetError"]?.ToString()))
        {
            <div class="alert alert-error login-alert" role="alert">
                <i class="fas fa-exclamation-circle"></i>
                @ViewData["ResetError"]
            </div>
        }

        @if (!string.IsNullOrEmpty(ViewData["ResetMessage"]?.ToString()))
        {
            <div class="alert alert-success login-alert" role="status">
                <i class="fas fa-check-circle"></i>
                @ViewData["ResetMessage"]
            </div>
        }

        <form method="post" action="/Account/ForgotPassword" class="login-form">
            @Html.AntiForgeryToken()

            <div class="input-group">
                <label class="input-label">
                    <i class="fas fa-envelope"></i>
                    Correo electrónico
                </label>
                <input type="email"
                       class="input"
                       name="email"
                       placeholder="tu@correo.com"
                       required
                       autocomplete="email" />
            </div>

            <button type="submit" class="btn btn-primary btn-block btn-lg login-submit-btn">
                <i class="fas fa-paper-plane"></i>
                Enviar enlace de recuperación
            </button>

            <a href="/Account/Login" class="btn btn-outline btn-block">
                <i class="fas fa-arrow-left"></i>
                Volver al inicio de sesión
            </a>
        </form>
    </div>
</div>
